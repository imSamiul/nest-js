info:
  name: Get Hello
  type: http
  seq: 1

http:
  method: GET
  url: '{{API_URL}}/'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # GET /

  Calls the application service through the root controller.

  ## Mapping
  - Controller: `AppController`
  - Handler: `getHello()`
  - Dependency: `AppService`

  ## Success
  - Status: 200
  - Body: `Hello World!`
