info:
  name: Get Hello
  type: http
  seq: 0

http:
  method: GET
  url: '{{API_URL}}/'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # GET /

  Health / bootstrap check for `AppController.getHello()`.

  ## Mapping
  - Method: GET
  - Route: `/`
  - Handler: `getHello()`

  ## Inputs
  None.

  ## Success
  - Status: 200
  - Body (text): `Hello World!`
